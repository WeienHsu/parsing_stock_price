### generate .exe file in Windows OS

$ pyinstaller -F parsing_me.py

### execute .exe with cacert.pem in same folder, and it will generate excel file.


### generate macOS dist file

$ pyinstaller --onefile --windowed --icon=/Users/weienhsu/Downloads/bagofmoney_5108.ico parsing_and_upload_to_drive.py
